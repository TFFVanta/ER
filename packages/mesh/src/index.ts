export interface MeshNode { id: string; name: string; type: "desktop | phone | server; status: online | offline; latency: number; } export const ExoticMesh = { nodes: [] as MeshNode[], register(node: MeshNode) { this.nodes.push(node); }, online() { return this.nodes.filter(n => n.status === online); }, summary() { return { total: this.nodes.length, online: this.online().length }; } };
