export interface NetworkState { connected: boolean; latencyMs: number; jitterMs: number; packetLoss: number; bandwidthMbps: number; stabilityScore: number; } export const ExoticNetwork = { current(): NetworkState { return { connected: true, latencyMs: 22, jitterMs: 4, packetLoss: 0.0, bandwidthMbps: 120, stabilityScore: 0.97 }; }, score(state: NetworkState) { return state.connected ? state.stabilityScore : 0; } };
