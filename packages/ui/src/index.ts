export type ExoticButton = { id: string; label: string; icon: string }; export const ExoticUI = { theme: { name: "black-white-static-enchanted, background: #000000, foreground: #ffffff, border: #ffffff, sharp: true, staticTexture: true, enchanted: true }, buttons: [ { id: dashboard, label: Dashboard, icon: ? }, { id: optimizer, label: Optimizer, icon: ? }, { id: observer, label: Observer, icon: ? }, { id: mesh, label: Mesh, icon: ? }, { id: device, label: Device, icon: ? }, { id: network, label: Network, icon: ? }, { id: settings, label: Settings, icon: ? } ] as ExoticButton[] };
