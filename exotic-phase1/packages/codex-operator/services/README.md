# Services

Service registration, dependency ordering, and health observation are implemented in:

- `src/services/service_registry.cpp`
- `src/lifecycle/lifecycle_manager.cpp`

This directory is reserved for future service manifests and operator-owned runtime adapters.
