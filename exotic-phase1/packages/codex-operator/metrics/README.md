# Metrics

Metrics collection is implemented in `src/metrics/metrics.cpp`.

Tracked foundation metrics include startup time, memory usage, active services, active workers, event throughput, error count, and extensible build statistics.
