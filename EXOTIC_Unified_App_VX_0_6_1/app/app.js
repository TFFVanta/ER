const KEY='exotic.unified.v061';const defaults={theme:'remedy',projects:[{id:'p1',name:'Exotic Platform',description:'Core architecture and runtime',tasks:[{id:'t1',title:'Windows desktop shell',done:true},{id:'t2',title:'Portal connection',done:false}]},{id:'p2',name:'Exotic Mobile',description:'Android and installable mobile workspace',tasks:[{id:'t3',title:'PWA shell',done:true},{id:'t4',title:'Device continuity',done:false}]}],workflows:[{id:'w1',name:'Build → Verify',description:'Check completed work and verify output.',enabled:true},{id:'w2',name:'Continuity Snapshot',description:'Prepare workspace state for sync.',enabled:false}]};let state=JSON.parse(localStorage.getItem(KEY)||'null')||structuredClone(defaults);let selected=state.projects[0]?.id||null;const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);function esc(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function pct(p){return p.tasks.length?Math.round(p.tasks.filter(t=>t.done).length/p.tasks.length*100):0}function save(){localStorage.setItem(KEY,JSON.stringify(state));render()}function showView(name){$$('.view').forEach(v=>v.classList.remove('active'));$('#'+name+'View').classList.add('active');$$('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===name));$('#pageTitle').textContent=name[0].toUpperCase()+name.slice(1);$('#sidebar').classList.remove('open');window.scrollTo({top:0,behavior:'smooth'})}function render(){document.body.dataset.theme=state.theme==='dark'?'dark':state.theme==='light'?'light':'remedy';const tasks=state.projects.flatMap(p=>p.tasks),done=tasks.filter(t=>t.done).length;$('#projectCount').textContent=state.projects.length;$('#completion').textContent=(tasks.length?Math.round(done/tasks.length*100):0)+'%';$('#openTasks').textContent=tasks.length-done;$('#projectList').innerHTML='';$('#projectGrid').innerHTML='';state.projects.forEach(p=>{const row=document.createElement('div');row.className='project'+(p.id===selected?' selected':'');row.innerHTML=`<b>${esc(p.name)}</b><small>${esc(p.description||'No description')}</small><div class="meta"><span>${p.tasks.filter(t=>!t.done).length} open</span><span>${pct(p)}%</span></div>`;row.onclick=()=>{selected=p.id;render()};$('#projectList').appendChild(row);const card=document.createElement('article');card.className='projectCard';card.innerHTML=`<h2>${esc(p.name)}</h2><small>${esc(p.description||'No description')}</small><div class="meter"><i style="width:${pct(p)}%"></i></div><div class="meta"><span>${p.tasks.length} tasks</span><span>${pct(p)}%</span></div><button>Open</button>`;card.querySelector('button').onclick=()=>{selected=p.id;showView('studio');render();setTimeout(()=>$('#projectDetail').scrollIntoView({behavior:'smooth'}),50)};$('#projectGrid').appendChild(card)});const p=state.projects.find(x=>x.id===selected);$('#projectDetail').classList.toggle('hidden',!p);if(p){$('#detailName').textContent=p.name;$('#detailDescription').textContent=p.description||'No description';$('#detailProgress').style.width=pct(p)+'%';$('#taskList').innerHTML='';p.tasks.forEach(t=>{const r=document.createElement('div');r.className='task'+(t.done?' done':'');r.innerHTML=`<input type="checkbox" ${t.done?'checked':''}><span>${esc(t.title)}</span><button>×</button>`;r.querySelector('input').onchange=e=>{t.done=e.target.checked;save()};r.querySelector('button').onclick=()=>{p.tasks=p.tasks.filter(x=>x.id!==t.id);save()};$('#taskList').appendChild(r)})}$('#workflowList').innerHTML='';state.workflows.forEach(w=>{const e=document.createElement('article');e.className='workflow';e.innerHTML=`<div class="workflowHead"><h2>${esc(w.name)}</h2><button class="${w.enabled?'on':'off'}">${w.enabled?'Enabled':'Disabled'}</button></div><p>${esc(w.description||'No description')}</p><button class="deleteW">Delete</button>`;e.querySelector('.workflowHead button').onclick=()=>{w.enabled=!w.enabled;save()};e.querySelector('.deleteW').onclick=()=>{state.workflows=state.workflows.filter(x=>x.id!==w.id);save()};$('#workflowList').appendChild(e)})}function openProject(){$('#projectModal').classList.remove('hidden');$('#projectName').focus()}$('#newProject').onclick=openProject;$('#newProject2').onclick=openProject;$('#cancelProject').onclick=()=>$('#projectModal').classList.add('hidden');$('#projectForm').onsubmit=e=>{e.preventDefault();const id='p'+Date.now();state.projects.push({id,name:$('#projectName').value.trim(),description:$('#projectDescription').value.trim(),tasks:[]});selected=id;$('#projectForm').reset();$('#projectModal').classList.add('hidden');save()};$('#taskForm').onsubmit=e=>{e.preventDefault();const p=state.projects.find(x=>x.id===selected);if(!p)return;p.tasks.push({id:'t'+Date.now(),title:$('#taskInput').value.trim(),done:false});$('#taskInput').value='';save()};$('#deleteProject').onclick=()=>{if(confirm('Delete project?')){state.projects=state.projects.filter(p=>p.id!==selected);selected=state.projects[0]?.id||null;save()}};$('#newWorkflow').onclick=()=>$('#workflowModal').classList.remove('hidden');$('#cancelWorkflow').onclick=()=>$('#workflowModal').classList.add('hidden');$('#workflowForm').onsubmit=e=>{e.preventDefault();state.workflows.push({id:'w'+Date.now(),name:$('#workflowName').value.trim(),description:$('#workflowDescription').value.trim(),enabled:true});$('#workflowForm').reset();$('#workflowModal').classList.add('hidden');save()};$$('[data-view]').forEach(b=>b.onclick=()=>showView(b.dataset.view));$('#menuBtn').onclick=()=>$('#sidebar').classList.toggle('open');$$('[data-theme]').forEach(b=>b.onclick=()=>{state.theme=b.dataset.theme;save()});function exportData(){const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='exotic-workspace-v061.json';a.click();URL.revokeObjectURL(a.href)}function importData(){$('#fileInput').click()}$('#exportBtn').onclick=exportData;$('#exportSettings').onclick=exportData;$('#importBtn').onclick=importData;$('#importSettings').onclick=importData;$('#fileInput').onchange=e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const d=JSON.parse(r.result);if(!Array.isArray(d.projects)||!Array.isArray(d.workflows))throw Error();state=d;selected=state.projects[0]?.id||null;save();alert('Workspace imported.')}catch{alert('Invalid workspace file.')}};r.readAsText(f)};$('#resetBtn').onclick=()=>{if(confirm('Reset workspace?')){state=structuredClone(defaults);selected=state.projects[0].id;save()}};setTimeout(()=>$('#splash').classList.add('hide'),900);setTimeout(()=>$('#splash').remove(),1450);render();